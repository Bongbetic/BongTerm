name: ci
on:
  pull_request:
  push:
    branches: [master, main]
  workflow_dispatch:

jobs:
  correctness:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust 1.95
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.95"
          components: rustfmt,clippy

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked

      - name: fmt
        run: cargo fmt --all -- --check

      - name: clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: test
        run: cargo test --workspace

      - name: deny check
        run: cargo deny check

      - name: xtask check-deps
        run: cargo xtask check-deps

      - name: xtask forbidden-abstraction
        run: cargo xtask forbidden-abstraction

      - name: xtask sbom
        run: cargo xtask sbom

      - name: xtask attestation
        run: cargo xtask attestation

      - name: xtask package-msix manifest smoke
        run: cargo xtask package-msix

      - name: build release smoke
        run: cargo build --release --workspace

      - name: submodule cleanliness
        shell: pwsh
        run: |
          $status = git submodule status
          if ($status -match "^[+\-]") {
            Write-Error "submodule dirty or uninitialized"
            exit 1
          }
